@model KitapciApp.EntityLayer.Satıs
@{
    ViewBag.Title = "Satış Sil";
}

<h2>Satış Sil</h2>

<hr />
<p>@Model.TarihSaat tarih-saatli satışı silmek istiyor musunuz?</p>

@using (Html.BeginForm("Sil", "Satis", FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()

    <div class="form-group">
        <div class="col-md-10">
            <a href="@Url.Action("Listele")" class="btn btn-primary">Hayır</a>
            <button type="submit" class="btn btn-danger">Evet</button>
        </div>
    </div>
}
